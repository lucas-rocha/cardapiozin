const baseDir = 'https://cardapiozin.s3.sa-east-1.amazonaws.com/images';
const usersPath = `${baseDir}/users/`;
const categoriesPath = `${baseDir}/categories/`;
const menuItemsPath = `${baseDir}/menu-items/`;
const restaurantsPath = `${baseDir}/restaurants/`;

export const paths = {
    usersPath,
    categoriesPath,
    menuItemsPath,
    restaurantsPath
};